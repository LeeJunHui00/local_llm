# 사내 LLM 개인 활용 가이드 (C/C++ 네이티브)

폐쇄망 사내 LLM 을 본인 개발 업무에 붙이기 위한 실전 가이드.
조직 도입 문서가 아니라 **내 워크플로우를 바꾸는 것**이 목적임.

전제: 이 LLM 은 Claude 와 마찬가지로 **CLI 코딩 에이전트 / MCP / OpenAI 호환 API** 를 지원함.
연결 대상은 **사내 소스코드와 Git**.

---

## 0. 세 줄 요약

1. **tool calling 지원 여부를 먼저 확인할 것.** 안 되면 MCP·에이전트는 전부 무의미함 (`setup/capability-check.py`)
2. **네이티브 업무의 최대 효과는 크래시 분석 자동화**임. 코어덤프 → 백트레이스 → 소스 대조를 에이전트가 스스로 돌게 만드는 것 (`mcp/gdb_mcp.py`)
3. **규칙 파일에 투자할 것.** 사내 모델은 상용 모델보다 약할 가능성이 높고, 약한 모델일수록 규칙 파일의 효과가 커짐 (`setup/PROJECT_RULES.md`)

무엇에 쓸지 먼저 정하려면 1장, MCP 가 처음이면 5장부터 볼 것.

---

## 1. 사내 모델이어야만 하는 작업

**차별점은 성능이 아니라 데이터 반출 제약임.** 사내 모델은 상용 모델보다 약할 가능성이 높으므로,
외부에서도 되는 일을 사내 모델로 하면 손해임. **외부로 못 보내는 것**에만 집중해야 값어치가 나옴.

### 1-1. 반출이 원천 금지된 데이터 ★ 가장 큼

**코어덤프가 대표 사례임.** 프로세스 메모리 전체 스냅샷이라 세션 키, 인증 토큰, 고객 데이터가
평문으로 들어 있음. 어떤 회사도 외부 LLM 에 올릴 수 없음. 그런데 네이티브 디버깅에서
LLM 도움이 가장 절실한 자료가 바로 이것임 — **못 하던 일이 가능해지는 유일한 지점.**

| 자료 | 왜 외부 불가 |
|---|---|
| 코어덤프 / 힙 덤프 | 메모리에 자격증명·고객 데이터가 그대로 들어 있음 |
| 실제 운영 로그 | 계정정보, 거래내역, PII |
| pcap 패킷 캡처 | 실 트래픽 페이로드 |
| 고객사 제공 재현 데이터 | NDA 대상 |
| 규제 자산 코드 | 방산·금융·의료 반출 금지 |

### 1-2. 폐쇄망 안에서만 도달 가능한 시스템

외부 LLM 은 **애초에 네트워크가 안 닿음.** MCP 로 붙일 수 있는 게 여기 다 있음 —
형상관리, 이슈트래커, 빌드 팜, CI 로그, 모니터링/APM, 장애 이력 DB, 타깃 보드.

1-1 과 1-2 를 조합하면 외부로는 구조적으로 불가능한 작업이 나옴:

> "어제 야간 회귀에서 실패한 케이스 전부 가져와서, 코어덤프 백트레이스 지문으로 묶고,
> 최근 3일 커밋과 대조해서 원인 후보 커밋을 뽑아줘"

### 1-3. 비용이 0 에 수렴하는 전수 처리

사내 GPU 는 고정비, 상용 API 는 종량제임. 그래서 **"일부"가 아니라 "전부"** 를 돌릴 수 있음.

- 저장소 전체 함수에 대한 설명 인덱스 생성 (수만 개 단위)
- 모든 커밋 자동 리뷰 — 샘플링 없이
- 매 야간 빌드마다 전체 정적 분석 결과 해석
- 크래시 리포트 전수 분류·중복 병합

상용 API 로는 비용이 감당 안 되는 규모임. 여기서 효과를 보려면 대화형이 아니라
**배치**로 짜야 함 (9장).

### 1-4. 모델 자체를 소유해서 생기는 것

- **파인튜닝** — 사내 API·매크로·약어를 학습시킴. 사내 컨벤션을 설명 없이 아는 모델이 됨
- **버전 고정** — 상용은 모델이 말없이 바뀜. 릴리스 심사 기록처럼 "그때 그 판단"을
  재현해야 하는 용도엔 고정 가능한 모델이 필수임
- **임베딩 직접 사용** — 코드 유사도 검색, 크래시 중복 판정은 생성 없이 임베딩만으로 됨.
  훨씬 싸고 빠름

### 1-5. 보안

취약점 분석, 미공개 결함 정보, 사내 보안 감사. 정의상 외부로 못 나감.

### 우선순위

1. **1-1 + 1-2 조합 (코어덤프 + 사내 시스템)** — `mcp/gdb_mcp.py` 가 이걸 겨냥한 것임. 최우선
2. **1-3 (전수 배치)** — `batch/review_diff.py` 를 배치로 확장. 투입 대비 효과 큼
3. **1-4 (파인튜닝)** — 운영팀 협의 필요. 장기 과제

**반대로 사내 모델로 하지 말아야 할 것**: 일반 프로그래밍 질문, 알고리즘 설명,
오픈소스 라이브러리 사용법. 외부 모델이 훨씬 나은데 굳이 약한 모델을 쓸 이유가 없음.

---

## 2. 시작 순서

| 단계 | 할 일 | 산출물 |
|---|---|---|
| 0 | 사내 LLM 스펙 점검 | `setup/capability-check.py` 실행 |
| 1 | 접속 환경변수 세팅 | `setup/env.sh.example` → `~/.llm-env` |
| 2 | 저장소에 규칙 파일 배치 | `setup/PROJECT_RULES.md` → 저장소 루트 |
| 3 | MCP 서버 등록 | `mcp/README.md` (개념은 5장) |
| 4 | 실전 패턴 적용 | 이 문서 6장 |
| 5 | 배치 자동화 | `batch/review_diff.py` |

**분기점**: 0단계에서 tool calling 이 미지원이면 3단계를 건너뛰고 6장(프롬프트 패턴) + 5단계로 감.

---

## 3. Phase 0 — 스펙 점검이 왜 먼저인가

```bash
source ~/.llm-env
python3 setup/capability-check.py --model <모델ID>
```

확인하는 항목과 그것이 결정하는 것:

| 항목 | 이게 없으면 |
|---|---|
| **tool / function calling** | MCP·에이전트 전부 불가. 프롬프트 붙여넣기 수준으로 회귀 |
| 컨텍스트 윈도우 | 좁으면 파일 통째 투입 불가 → 도구 출력 상한을 줄여야 함 |
| system prompt 반영 | 안 되면 규칙 파일이 무시됨 → 매 요청마다 규칙을 본문에 넣어야 함 |
| streaming | 없으면 CLI 에이전트 체감이 크게 나빠짐 |
| 모델 목록 / rate limit | 배치 자동화 규모와 병렬도를 결정 |

컨텍스트 실측까지 하려면:

```bash
python3 setup/capability-check.py --model <모델ID> --context-probe 8000,32000,128000
```

**공표 스펙을 믿지 말고 실측할 것.** "128k 지원"이라고 해도 중간 구간의 정보를 못 꺼내는
경우가 흔함. needle 회수 테스트가 실패하는 크기부터는 실사용 불가로 봐야 함.

---

## 4. 폐쇄망 세팅에서 실제로 막히는 지점

기능 문제보다 **인증서와 프록시**에서 시간을 더 많이 씀. `setup/env.sh.example` 에 다 넣어뒀지만
요지만 적으면:

- 사내 게이트웨이는 프록시를 **거치면 안 되는** 경우가 많음 → `NO_PROXY` 에 반드시 포함
- 사설 CA 로 TLS 종단하면 언어별로 인증서 변수를 따로 줘야 함
  (`SSL_CERT_FILE`, `REQUESTS_CA_BUNDLE`, `NODE_EXTRA_CA_CERTS`, `CURL_CA_BUNDLE`)
- OpenAI 호환만 제공되는데 에이전트가 Anthropic 규격만 받으면 변환 프록시가 필요함.
  **폐쇄망이니 wheel 을 미리 반입해야 함** — 반입 신청 목록에 넣어둘 것

여기 있는 스크립트와 MCP 서버는 전부 **표준 라이브러리만** 씀. 패키지 반입 없이 바로 돌아감.

---

## 5. MCP 란 무엇인가

처음 접한다면 **LSP(clangd 같은 것)** 를 떠올리면 거의 정확함. MCP 는 LSP 설계를 그대로
따왔음 — 별도 프로세스를 띄우고 stdin/stdout 으로 JSON-RPC 를 주고받는 것까지 같음.

### 왜 필요한가

LLM 은 **텍스트만 생성함.** 파일도 못 읽고 gdb 도 못 돌림. 그래서 이렇게 우회함:

> 모델에게 "쓸 수 있는 도구 목록"을 알려주고 → 모델이 "이 도구를 이 인자로 호출해줘"라고
> **텍스트로 요청**하면 → **바깥에서 대신 실행**해주고 → 결과를 다시 모델에게 넣어줌

MCP 이전엔 도구 붙이는 방식이 제품마다 제각각이었음. MCP 는 이걸 표준화해서,
서버를 한 번 만들면 여러 에이전트에 그대로 재사용됨.

### 한 턴에서 실제로 벌어지는 일

이게 핵심 개념임. "이 코어덤프 원인 찾아줘"라고 쳤을 때:

```
사용자 ──> 에이전트
              │  ① 모델에게 보냄:
              │     사용자 메시지 + 사용 가능한 도구 목록(JSON 스키마)
              ▼
            모델
              │  ② 답을 냄. 텍스트가 아니라 구조화된 호출 요청:
              │     "load_core 를 {binary:..., core:...} 로 호출해줘"
              ▼
            에이전트 ──③ tools/call──> MCP 서버 (별도 프로세스)
                                          │ ④ 실제로 gdb 실행
              에이전트 <──결과 텍스트──────┘
              │  ⑤ 결과를 대화에 덧붙여 모델에게 다시 보냄
              ▼
            모델  ⑥ 결과를 보고 다음 도구를 호출하거나, 최종 답을 냄
                     (②~⑥ 반복 = "에이전트 루프")
```

**모델은 절대 직접 실행하지 않음. 요청만 함.** 실행은 에이전트가 함. 그래서 중간에
"이 명령 실행할까요?" 승인이 끼어들 수 있는 것임.

②단계에서 모델이 구조화된 호출을 낼 수 있어야 하는데 이게 **tool calling** 임.
3장에서 이 지원 여부를 최우선으로 확인하라고 한 이유가 여기 있음 — 없으면 ②에서 루프가 끊김.

### 실제 오가는 바이트

메서드는 셋뿐임.

| 메서드 | 역할 | 시점 |
|---|---|---|
| `initialize` | 버전·기능 협상 (LSP 와 동일) | 프로세스 시작 시 1회 |
| `tools/list` | "무슨 도구가 있냐" | 초기화 직후 1회 |
| `tools/call` | "이 도구를 이 인자로 실행해라" | 매번 |

```jsonc
// --> 에이전트가 보냄
{"jsonrpc":"2.0","id":2,"method":"tools/call",
 "params":{"name":"find_symbol","arguments":{"name":"freeSession"}}}

// <-- 서버가 답함
{"jsonrpc":"2.0","id":2,"result":{"content":[{"type":"text",
 "text":"freeSession\tsrc/session.c\tf\t..."}]}}
```

응답이 결국 `{"type":"text"}` 인 게 핵심임. **모델에게 들어가는 건 텍스트뿐**이므로
출력을 짧고 구조적으로 만들어야 함 — `build_mcp.py` 에서 원문 로그 대신 `parse_errors` 로
정규화한 이유가 이것임.

### 도구 설명이 곧 프롬프트임

모델은 도구의 `description` 만 보고 **언제 쓸지 스스로 판단함.** 그래서 서버 코드의
설명을 길게 써뒀음:

```python
@srv.tool(
    "ctags 인덱스에서 심볼 정의 위치를 조회함. 함수/구조체/매크로 정의를 찾을 때 "
    "가장 먼저 쓸 것. 인덱스가 없으면 build_tags 를 먼저 호출.",
    ...
)
def find_symbol(name, prefix=False):
```

"가장 먼저 쓸 것", "build_tags 를 먼저 호출" 같은 문구가 실제로 도구 선택을 바꿈.
**API 문서가 아니라 모델에게 주는 지시문**으로 써야 함.

### 흔한 오해

| 오해 | 실제 |
|---|---|
| MCP 서버가 LLM 을 호출한다 | 반대임. 모델이 서버를 씀. 서버는 LLM 을 전혀 모름 |
| MCP 서버는 웹서버다 | 로컬은 stdin/stdout 붙은 프로세스임. 포트도 안 열림 |
| 모델이 명령을 실행한다 | 요청만 함. 실행은 에이전트. 그래서 통제가 가능 |

### 직접 해볼 것

`mcp/_mcplib.py` 가 **148줄이고 프로토콜 구현 전체**임. 한 번 읽으면 끝임.

```bash
export LLM_REPO_ROOT=~/work/myproject

# 서버가 자기소개하고 도구 목록 내놓는 것 보기
python3 mcp/smoke_test.py repo_mcp.py

# 도구 하나 직접 때려보기 (에이전트 없이)
python3 mcp/smoke_test.py repo_mcp.py --call find_symbol '{"name":"main"}'
```

에이전트에 붙이기 전에 이걸로 서버만 따로 검증하는 습관을 들일 것.
에이전트 쪽에서는 실패 원인이 안 보임.

### 더 알아두면 좋은 것 (당장은 몰라도 됨)

- **전송 방식** — 로컬은 stdio, 원격은 HTTP. 사내 공용 도구를 만들면 HTTP 로 올릴 수 있음
- **3가지 제공물** — `tools`(실행), `resources`(읽기 자료), `prompts`(정형 프롬프트).
  실무에서 쓰는 건 사실상 `tools` 뿐임

---

## 6. C/C++ 실전 활용 패턴

효과 큰 순서. 각 항목은 *언제 / 어떻게 / 주의점* 구성.

### 6-1. 크래시·코어덤프 분석 ★ 최우선

**언제**: 필드 크래시 리포트, 야간 테스트 SIGSEGV, 재현 안 되는 간헐 장애

**어떻게**: `gdb-mcp` 를 붙이면 한 문장으로 전 과정이 돌아감.

```
/var/crash/core.myapp.1234 를 build/bin/myapp.debug 로 분석해서
크래시 원인 후보를 찾아줘. 의심 프레임의 지역변수까지 확인하고,
관련 소스는 repo 도구로 직접 열어볼 것.
```

에이전트가 도는 경로: `load_core` → `backtrace` → `frame(n)` → `print_expr` →
(repo-mcp) `find_symbol`/`read_file` → `git_blame` 으로 도입 커밋까지.

MCP 없이 API 만 쓸 때는 이 정도만 붙여도 충분함:

```bash
gdb -batch -nx -ex "set pagination off" -ex "bt 40" -ex "frame 0" \
    -ex "info args" -ex "info locals" -ex "info registers" \
    build/bin/myapp.debug /var/crash/core.myapp.1234
```

**주의점**
- 배포 바이너리는 strip 되어 있음. **심볼 포함본 경로를 규칙 파일에 명시**해두지 않으면
  백트레이스가 `??` 만 나오고 세션이 통째로 낭비됨
- `-O2` 빌드는 인라인·변수 최적화로 지역변수가 `<optimized out>` 이 됨.
  이때는 `disas` 로 넘어가야 하며, 모델의 추정 신뢰도도 함께 떨어짐
- 모델이 내놓는 건 **원인 후보**지 결론이 아님. 특히 "메모리 손상 같다" 류의 답은
  근거 프레임을 되물어서 검증할 것

### 6-2. ASAN / valgrind 리포트 해석

**언제**: 리포트는 나왔는데 어느 코드 경로에서 온 건지 바로 안 보일 때

**어떻게**: 리포트 원문 + 관련 함수 소스를 같이 주고, 판정을 요구함.

```
아래 ASAN heap-use-after-free 리포트다. 해제 스택과 사용 스택을 대조해서
어느 소유권 규칙이 깨졌는지 설명하고, 수정 방향을 코드 블럭으로 제시할 것.
소스가 더 필요하면 repo 도구로 직접 열 것.
```

**주의점**: ASAN 리포트는 그 자체로 증거가 강해서 모델 정확도가 높은 편임.
반면 valgrind 의 "possibly lost" 류는 오탐이 많으니 그대로 믿지 말 것.

### 6-3. 빌드 에러 일괄 해소

**언제**: 템플릿 인스턴스화 에러, 링커 에러처럼 **사람이 읽기 힘든** 에러.
단순 오타 에러는 그냥 직접 고치는 게 빠름.

**어떻게**: `build-mcp` 의 `parse_errors` 를 쓰게 함.

```
빌드하고 에러를 구조화해서 확인한 뒤, 원인별로 묶어서 수정안을 제시할 것.
원문 로그 대신 parse_errors 를 쓸 것.
```

`parse_errors` 가 원문 대신 `{file, line, severity, message}` 로 정규화해서 넘김.
수천 줄짜리 템플릿 백트레이스를 통째로 넣는 것보다 **토큰도 줄고 정확도도 올라감.**

**주의점**: 링커 에러(`undefined reference`)는 빌드 시스템 설정 문제인 경우가 많은데,
모델은 소스 수정으로 해결하려는 경향이 있음. CMake 설정을 같이 보게 할 것.

### 6-4. 레거시 모듈 독해

**언제**: 문서 없는 모듈을 처음 맡았을 때. **투입 대비 효과가 큰데 가장 안 쓰이는 용도**임.

**어떻게**:

```
src/net/ 모듈의 구조를 파악해줘. 진입점 함수, 상태 머신, 다른 모듈과의 경계,
그리고 수정 시 주의할 지점을 정리할 것. repo 도구로 심볼과 호출 관계를 직접 확인할 것.
```

산출물을 그대로 규칙 파일의 "소스 트리" 절에 반영하면 이후 세션 품질이 계속 올라감.

**주의점**: 모델이 못 읽은 파일에 대해 그럴듯하게 지어냄. 요약에 나온 함수명은
`find_symbol` 로 실재 여부를 한 번 확인할 것.

### 6-5. diff 기반 코드리뷰

**언제**: 커밋 전 셀프 리뷰, 남의 MR 볼 때 1차 스크리닝

**어떻게**: 대화형이면 에이전트에 `git_diff` 를 쓰게 하고, 배치면 `batch/review_diff.py`.

체크리스트를 **고정**하는 게 핵심임. 자유롭게 두면 스타일 지적만 잔뜩 나옴.
`batch/review_diff.py` 의 시스템 프롬프트에 다음만 남겨뒀음:
메모리 / 버퍼 / UB / 에러 경로 / 동시성 / 이식성.

```bash
python3 batch/review_diff.py --staged
python3 batch/review_diff.py --base origin/main
```

**주의점**: LLM 리뷰는 **사람 리뷰의 대체가 아니라 1차 필터**임. 특히 diff 만 보면
호출자 문맥이 없어서 오탐이 나옴. 그래서 출력에 `confidence` 와 `needs`(추가로 봐야 할 파일)를
같이 받도록 해둠 — 확신도 낮은 지적은 파일을 더 열어보고 판단할 것.

### 6-6. 테스트 골격 생성

**언제**: 기존 함수에 테스트를 뒤늦게 붙일 때

**어떻게**: 함수 소스 + 기존 테스트 파일 하나를 같이 주고 스타일을 맞추게 함.

```
sessionLength() 에 대한 gtest 케이스를 작성해줘.
tests/session_test.cc 의 기존 스타일을 따를 것.
경계값(빈 문자열, 최대 길이, 널 포인터)을 반드시 포함할 것.
```

**주의점**: 모델이 만든 테스트는 **통과하는 것만 확인하지 말고 일부러 깨뜨려볼 것.**
아무 assert 도 실제로 검증하지 않는 껍데기 테스트가 자주 나옴.

### 6-7. 표준·컴파일러 버전 상향 포팅

**언제**: C++11 → 17, gcc 버전 업으로 경고가 에러로 승격됐을 때

**어떻게**: 기계적 변환이라 대량 처리에 적합함. 파일 단위로 끊어서 돌리고 매번 빌드로 검증.

**주의점**: 한 번에 여러 파일을 바꾸게 하면 검증이 불가능해짐. **파일 하나 → 빌드 → 다음 파일**
리듬을 유지할 것.

### 6-8. 일회성 분석 도구 만들기

**언제**: 로그 파싱, 크래시 리포트 집계, 바이너리 포맷 덤프 같은 **버릴 스크립트**

가장 안전한 용도임. 결과가 눈으로 바로 검증되고, 틀려도 손실이 없음.
"이 정도는 직접 짜지" 싶은 걸 넘기는 게 실제로 시간을 가장 많이 아낌.

---

## 7. 사내 모델의 한계를 전제로 한 사용법

상용 모델과 같은 기대를 하면 실망하고 안 쓰게 됨. 처음부터 다음을 전제할 것.
(약한 모델을 왜 굳이 쓰는가에 대한 답은 1장에 있음 — 성능이 아니라 데이터 접근이 이유임.)

**컨텍스트를 아껴 쓸 것**
- 파일 통째 대신 관련 함수만. `read_file` 에 줄 범위를 주는 이유임
- 도구 출력 상한(`LLM_MCP_MAX_OUTPUT`)을 컨텍스트에 맞춰 조정
- 세션이 길어지면 새로 시작. 앞부분을 이미 흘렸을 가능성이 큼

**태스크를 잘게 쪼갤 것**
- "이 모듈 리팩터링해줘" (X) → "이 함수의 에러 처리 경로만 정리해줘" (O)
- 한 번에 한 파일. 검증 가능한 단위로 끊는 게 핵심

**출력은 반드시 기계로 검증할 것**
- 컴파일 → 테스트 → (가능하면) ASAN 실행까지
- 모델이 "수정했습니다"라고 한 것과 실제로 동작하는 것은 별개임

**모델이 잘 틀리는 것**
- 프로젝트 고유 API 시그니처를 그럴듯하게 지어냄 → `find_symbol` 로 확인
- 존재하지 않는 헤더/매크로를 씀 → 빌드로 걸러짐
- 확신에 찬 어투로 틀림 → **어투와 정확도는 무관**함. 근거를 되물을 것

**반대로 사내 모델이 유리한 점**
- 코드를 외부로 보내지 않으므로 **전체 저장소를 인덱싱해도 무방**함.
  상용 서비스에서는 못 하던 걸 할 수 있는 유일한 지점이니 적극 활용할 것

---

## 8. 규칙 파일 — 여기가 품질의 8할

`setup/PROJECT_RULES.md` 를 저장소 루트에 복사해서 채울 것.

작성 원칙:
- **추측하면 틀리는 것만** 적음 (심볼 경로, 빌드 명령, 금지 패턴, 프로젝트 고유 함정)
- 코드를 읽으면 아는 내용은 넣지 않음 (토큰만 소모)
- 100줄 안쪽 유지. 넘어가면 뒷부분이 무시되기 시작함
- **한 번에 완성하려 하지 말 것.** 에이전트가 같은 실수를 두 번 하면 그때 한 줄 추가하는
  방식이 가장 효율적임

개인 취향(직접 수정 금지, 코드 블럭으로 제안, 함수명은 괄호 표기)도 여기 넣으면
매 세션 반복해서 말할 필요가 없어짐.

---

## 9. 배치 자동화

에이전트 없이 API 만으로 돌릴 수 있는 것들. `batch/review_diff.py` 를 원형으로 변형하면 됨.

| 만들 것 | 트리거 | 비고 |
|---|---|---|
| diff 코드리뷰 | 수동 / pre-commit | 이미 구현됨 |
| 커밋 메시지 초안 | `prepare-commit-msg` 훅 | staged diff → 메시지 생성 |
| 릴리스 노트 | 태그 시점 | `git log` 범위 → 사용자 관점 요약 |
| 크래시 리포트 분류 | 야간 배치 | 백트레이스 지문으로 중복 병합 + 유사 이슈 매칭 |
| 빌드 실패 요약 | CI 실패 시 | `parse_errors` 로직 재사용 |

훅으로 걸 때 원칙: **LLM 결과로 커밋이나 빌드를 막지 말 것.** 참고 정보로만 붙일 것.
LLM 이 게이트가 되는 순간 오탐 하나로 팀 전체가 막힘.

---

## 10. 첫 주 실행 순서

- **1일차** — `capability-check.py` 실행. tool calling 지원 여부 확인.
  안 되면 운영팀에 지원 계획을 문의할 것 (이게 없으면 활용 폭이 절반 이하로 줄어듦)
- **2일차** — 접속 세팅 완료, 규칙 파일 초안 작성 (빌드 명령 + 심볼 경로만이라도)
- **3일차** — `repo-mcp` 등록. `build_tags` 돌리고 코드 검색·심볼 조회부터 익힐 것
- **4일차** — `gdb-mcp` 등록. **과거에 이미 원인을 아는 코어덤프**로 먼저 검증할 것.
  정답을 아는 케이스로 모델의 정확도 감을 잡는 게 중요함
- **5일차** — `build-mcp` 등록, `review_diff.py` 를 실제 MR 에 돌려봄
- **이후** — 에이전트가 틀릴 때마다 규칙 파일에 한 줄씩 추가

---

## 11. 파일 안내

```
GUIDE.md                    이 문서
setup/env.sh.example        접속 환경변수 템플릿 (프록시·CA 포함)
setup/PROJECT_RULES.md      저장소용 규칙 파일 템플릿
setup/capability-check.py   사내 LLM 스펙 점검 (Phase 0)
mcp/README.md               MCP 등록·검증·문제 해결
mcp/_mcplib.py              의존성 없는 MCP stdio 구현
mcp/gdb_mcp.py              코어덤프 분석 (읽기 전용)
mcp/repo_mcp.py             코드 검색·심볼·git 이력 (읽기 전용)
mcp/build_mcp.py            빌드 실행·에러 구조화
mcp/smoke_test.py           MCP 서버 단독 검증
mcp/verify_gdb.sh           gdb MCP 실동작 검증
batch/review_diff.py        diff 코드리뷰 배치 (API 직접 호출)
```
